import React from 'react';
import './Spinner.css'; // Import the Spinner-specific CSS

const Spinner = () => {
  return (
    <div className="spinner">
      <div className="spinner-circle"></div>
    </div>
  );
};

export default Spinner;
